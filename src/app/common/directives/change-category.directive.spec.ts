import { ChangeCategoryDirective } from './change-category.directive';

describe('ChangeCategoryDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeCategoryDirective();
    expect(directive).toBeTruthy();
  });
});
